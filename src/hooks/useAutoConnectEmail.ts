/**
 * Hook stub — auto-connect email was removed.
 * Users should manually condnect their email/calendar from Settings > Integrations.
 */
export function useAutoConnectEmail() {
  // No-op: auto-connect removed; users connect manually via Settings.
}
