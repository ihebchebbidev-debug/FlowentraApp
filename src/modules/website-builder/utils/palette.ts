/**
 * Component Palette — Re-exports from palette/ directory.
 * Backward-compatible: existing `import { COMPONENT_PALETTE } from '../utils/palette'` still works.
 */
export { COMPONENT_PALETTE } from './palette/index';
export {
  LAYOUT_PALETTE,
  NAVIGATION_PALETTE,
  TEXT_PALETTE,
  MEDIA_PALETTE,
  BUSINESS_PALETTE,
  ECOMMERCE_PALETTE,
  INTERACTIVE_PALETTE,
  BLOG_PALETTE,
  ADVANCED_PALETTE,
} from './palette/index';
