/**
 * Calculation notes were removed from dashboards and reports.
 * Kept as a no-op so existing `explain` props remain valid.
 */
export const ExplainNote = (_props: {
  children?: React.ReactNode;
  variant?: 'card' | 'page';
  className?: string;
}) => null;
